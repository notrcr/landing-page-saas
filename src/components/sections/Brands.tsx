import { Container } from "../share/container";

export const Brands = () => {
    return (
        <section className="relative mt-12 md:mt-16">
            <Container className="flex justify-center align-center cursor-default">/
            </Container>
        </section>
    );
};